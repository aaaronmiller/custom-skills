#!/usr/bin/env bash
# prompt-mine-ingest — Run the daily ingestion pipeline
# Usage: prompt-mine-ingest [--all|--incremental|--status]

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON="${PYTHON:-python3}"

exec "$PYTHON" "$SCRIPT_DIR/../skills/mine/scripts/daily_ingest.py" "$@"
